# 華語知識王爭霸 🏆

一個即時線上華語知識競賽遊戲，讓學習華語變得更有趣！

## 🌟 功能特點

- **即時對戰**: 支援多人即時線上競賽
- **智慧配對**: 自動匹配實力相當的對手
- **計時競賽**: 每題8秒時間限制，考驗反應速度
- **多元題型**: 包含漢語拼音、中文會話、選擇題等
- **即時分數**: 競賽過程中即時顯示雙方分數
- **精美UI**: 現代化設計，支援響應式佈局
- **跨平台**: 支援桌面和移動端瀏覽器

## 🎮 遊戲規則

1. **進入遊戲**: 輸入暱稱後點擊「開始配對」
2. **等待配對**: 系統會自動尋找適合的對手
3. **開始競賽**: 雙方同時收到相同題目
4. **作答時間**: 每題有8秒時間限制
5. **即時計分**: 正確答案+10分，答錯或超時+0分
6. **勝負判定**: 題目全部作答完畢後，分數高者獲勝
7. **重新競賽**: 遊戲結束後可選擇重新開始

## 🛠 技術棧

- **前端框架**: Vue 3 (Composition API)
- **即時通訊**: WebSocket (STOMP.js + SockJS)
- **資料處理**: PapaParse (CSV解析)
- **建構工具**: Vue CLI
- **樣式**: 自訂CSS + Flexbox/Grid佈局

## 📋 系統需求

- Node.js >= 14.0.0
- npm >= 6.0.0
- 現代瀏覽器 (Chrome, Firefox, Safari, Edge)
- 後端服務器 (WebSocket支援)

## 🚀 安裝與運行

### 1. 下載專案

```bash
git clone https://github.com/your-username/quiz-battle.git
cd quiz-battle
```

### 2. 安裝依賴

```bash
npm install
```

### 3. 配置後端服務器

確保後端WebSocket服務器正常運行，並更新前端配置：

```javascript
// src/composables/useSocket.js
const socketUrl = 'ws://your-backend-server:port/ws'
```

### 4. 開發模式運行

```bash
npm run serve
```

訪問 `http://localhost:8080` 開始遊戲！

### 5. 生產環境建構

```bash
npm run build
```

建構後的檔案會在 `dist/` 目錄中。

## 📁 專案結構

```
quiz-battle/
├── public/                    # 靜態資源
│   ├── index.html            # HTML模板
│   └── question.csv          # 題庫資料
├── src/
│   ├── assets/               # 圖片等資源
│   ├── components/           # Vue組件
│   │   ├── AnswerButton.vue  # 答案按鈕
│   │   ├── PlayerPanel.vue   # 玩家資訊面板
│   │   ├── QuestionCard.vue  # 題目卡片
│   │   ├── ResultModal.vue   # 結果模態框
│   │   └── TimerBar.vue      # 計時器
│   ├── composables/          # Vue 3 Composition API
│   │   ├── useBattle.js      # 對戰邏輯
│   │   ├── useBattleGame.js  # 遊戲狀態管理
│   │   ├── useGame.js        # 通用遊戲邏輯
│   │   ├── useSocket.js      # WebSocket通訊
│   │   └── useTimer.js       # 計時器邏輯
│   ├── App.vue               # 根組件
│   └── main.js               # 應用入口
├── package.json              # 專案配置
└── vue.config.js             # Vue CLI配置
```

## 🎯 遊戲組件說明

### 核心組件

- **App.vue**: 主應用組件，處理遊戲狀態切換
- **QuestionCard.vue**: 題目顯示和答案選擇
- **PlayerPanel.vue**: 顯示玩家資訊和分數
- **TimerBar.vue**: 倒數計時視覺化
- **ResultModal.vue**: 遊戲結果展示

### 邏輯模組

- **useBattleGame.js**: 遊戲主要狀態管理
- **useSocket.js**: WebSocket連線和訊息處理
- **useTimer.js**: 計時器功能
- **useBattle.js**: 對戰相關邏輯

## 📝 題目格式

題庫使用CSV格式，支援以下欄位：

```csv
題目ID,題目,答案,選項1,選項2,選項3,選項4
1,"「老」拼音是什麼？",1,lǎo,láo,lào,lao
```

- **題目ID**: 唯一識別碼
- **題目**: 問題內容
- **答案**: 正確選項的編號 (1-4)
- **選項1-4**: 四個選擇項

## 🔧 開發指南

### 添加新題目

1. 編輯 `public/question.csv` 文件
2. 按照指定格式添加新題目
3. 重新載入應用即可生效

### 自訂主題

修改 `src/App.vue` 中的CSS變數來自訂顏色：

```css
:root {
  --primary-color: #4facfe;
  --secondary-color: #00f2fe;
  --background-gradient: linear-gradient(135deg, #4b134f 0%, #190a23 100%);
}
```

### WebSocket配置

在 `src/composables/useSocket.js` 中配置後端連接：

```javascript
const SERVER_URL = process.env.VUE_APP_WEBSOCKET_URL || 'ws://localhost:8080/ws'
```

## 🤝 貢獻指南

歡迎參與專案改進！

1. Fork 此專案
2. 建立功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交變更 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 開啟 Pull Request

### 開發規範

- 使用 ESLint 規範程式碼風格
- 組件使用 Vue 3 Composition API
- 遵循現有的命名慣例
- 新功能請附上適當的測試

## 📄 授權條款

本專案採用 MIT 授權條款 - 詳見 [LICENSE](LICENSE) 文件

## 🙏 致謝

感謝所有為華語學習做出貢獻的開發者和教育工作者！

---

**快來挑戰自己，成為華語知識王者吧！** 🌟
